# 3DsMIN

### Build steps

Clone repo with dependecies:

`git clone --recursive https://github.com/IgorPritula/3DsMIN.git`

Process cmake build:

`cd 3DsMIN`  
`mkdir build`  
`cd build`  
`cmake ../`  
`make `  
